import { SolutionDescriptValidatorDirective } from './solution-descript-validator.directive';

describe('SolutionDescriptValidatorDirective', () => {
    it('should create an instance', () => {
        const directive = new SolutionDescriptValidatorDirective();
        expect(directive).toBeTruthy();
    });
});
